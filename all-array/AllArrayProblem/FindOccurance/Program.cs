using AllArrayProblem;
using System;
using System.Collections.Generic;
using System.Collections.Immutable;
using System.Linq;

namespace FindOccurance
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int[] inputArray = { 2, 0, 1, 0, 2 };

            /*  Find Frequency
             *  var dataDictionary = FindFrequency.GetOccuranceDictionary(inputArray);
              foreach ( var data in dataDictionary )
              {
                  Console.WriteLine($"{data.Key} - {data.Value}");
              }
            */

            /*int secondLargest = SecondLargest.GetSecondLargest(inputArray);
            Console.WriteLine(secondLargest);*/

            /* var matrix = InputService.GetMatrix(3);

             var sum = SumLeftDiagonal.GetSumOfLeftDiagonal(matrix, 3);

             Console.WriteLine(sum);*/

            //Console.WriteLine(FindDigits.IsDigitExist(inputArray, "7"));

            //Console.WriteLine(SumOfPriceNumber.GetSumOfPrime(inputArray));

            // Console.WriteLine(MaximumSubarray.GetMaximumSubArray(inputArray));

            //Console.WriteLine(LongestConsecutive.GetLongestConsecutiveLength(inputArray));

            // Console.WriteLine(string.Join(" ", TwoSum2Functions.GetTwoSum(inputArray, 12)));

            //  (long minSum, long maxSum) result = MaxMinSumOfFour.maxSum(inputArray.ToList(), 5, 4);

            //   Console.WriteLine($"{result.minSum} {result.maxSum}");

            // Console.WriteLine(FavouriteSinger.GetCountOfFavouriteSinger(inputArray.ToList()));

            /* int[] arr1 = { 1, 2, 3, 4, 5 };
             int[] arr2 = { 1, 2, 5, 7, 9 };
             int[] arr3 = { 1, 3, 4, 5, 8 };   
            IntersectionOf3SortedArray.IntersectArray(5,5,5, arr1, arr2, arr3); 
            */

            // int[] inputArray = { -4, -1,0, 3, 10 };
            // List<int> restul = SortThoseSquareMethods.SortThoseSquare(inputArray);
            // Console.WriteLine(MissingNumber.GetMissingNumber(inputArray, 5));


            //Console.WriteLine(DestroyPairFunctions.DestroyPairs("abcddce"));

            // string[] words = { "hello", "leetcode" };
            // AlienDictionaryMethods alienDictionaryMethods = new AlienDictionaryMethods();
            //  alienDictionaryMethods.IsAlienSorted(words, "hlabcdefgijkmnopqrstuvwxyz");

            // LongestSubStringWithoutRepeat longestSubStringWithoutRepeat   = new LongestSubStringWithoutRepeat();
            // Console.WriteLine(longestSubStringWithoutRepeat.GetLongestSubStringWithoutRepeat("abcbcde"));

            // Console.ReadLine().Split(" ").Select(int.Parse).ToList();

          //  FreqSumMethods.FindFreqSum();

          // int[] outPut = OneTwosZerosSortFunctions.GetSortedArray(inputArray);

         
        }


    }
   
}
